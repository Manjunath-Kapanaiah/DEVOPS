name: CI Pipeline

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  simple-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Code Quality Check
        run: |
          echo "Scanning files for issues..."
          ls -R
          echo "All files are present and accounted for."

      - name: Run Dummy Test
        run: echo "Tests passed successfully!"
